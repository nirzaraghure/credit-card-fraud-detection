# System Architecture

```mermaid
graph TD
    root[.] -->|Entry Point| A[app.py]
    root -->|Dependencies| B[requirements.txt]
    root -->|Core Asset| C[rf_fraud_model.pkl]
    A -->|Core Logic| D[Fraud Detection]
    D -->|ML Model| C
    D -->|HTTP API| E[FastAPI]
    B -->|Libs| F[Flask/FastAPI]
    B -->|ML| G[Scikit-learn]
    B -->|Utils| H[Pandas/NumPy]
```

> Generated by GitGenius Sentinel