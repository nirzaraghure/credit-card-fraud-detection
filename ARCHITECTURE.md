# System Architecture

```mermaid
graph TD
    A[App] --> B[api]
    B[api] --> C[data]
    C[data] --> D[fraud_model]
    D[fraud_model] --> E[model]
    E[model] --> F[train]
    F[train] --> G[test]
    G[test] --> H[results]
    H[results] --> I[ui]
    I[ui] --> J[frontend]
    J[frontend] --> K[backend]
    K[backend] --> L[database]
    L[database] --> M[model]
    M[model] --> N[train]
    N[train] --> O[test]
    O[test] --> P[results]
    P[results] --> Q[ui]
    Q[ui] --> R[frontend]
    R[frontend] --> S[backend]
    S[backend] --> T[database]

    A -->|load data| D[fraud_model]
    B -->|process data| E[model]
    C -->|train model| F[train]
    D -->|save model| M[model]
    E -->|train model| N[train]
    F -->|test model| O[test]
    G -->|evaluate results| P[results]
    H -->|display results| Q[ui]
    I -->|render ui| R[frontend]
    J -->|handle requests| K[backend]
    K -->|store data| L[database]
    M -->|load model| N[train]
    N -->|train model| O[test]
    O -->|evaluate results| P[results]
    P -->|display results| Q[ui]
    Q -->|render ui| R[frontend]
    R -->|handle requests| K[backend]
    S -->|store data| L[database]
```

> Generated by GitGenius Sentinel